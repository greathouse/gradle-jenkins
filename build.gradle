apply plugin: 'groovy'
apply plugin: 'idea'

buildscript {
    repositories {
        maven {
            mavenCentral()
        }
    }
}

apply plugin: 'jenkins'

jenkins {
    console = true

    template {
        publishers {
            junitResultArchiver {
                testResults = 'Something'
            }
        }
    }
}